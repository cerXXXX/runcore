name: cross_cache
description: "Stub cross_cache implementation for runcore_app (no path_provider dependency)."
version: 1.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.4
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

